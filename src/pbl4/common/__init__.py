"""Common utilities shared across all PBL4 components.

This package contains pure helpers with zero dependencies on:
- PyTorch / deep learning frameworks
- Database libraries (psycopg, asyncpg, etc.)
- Web frameworks (FastAPI, uvicorn, etc.)
- Specific PBL4 subsystem implementations
  (protocol, runtime, management_backend, worker, dataset_manager)
"""

from __future__ import annotations
